# ============================================
# SABROTORTAS - Backend Landing
# Configuración de Variables de Entorno
# ============================================

# Puerto del servidor
PORT=5002

# Supabase Configuration
SUPABASE_URL=https://tu-proyecto.supabase.co
SUPABASE_SERVICE_ROLE_KEY=tu-service-role-key-aqui

# Email Configuration (Gmail)
EMAIL_USER=tu-email@gmail.com
EMAIL_PASS=tu-app-password-de-gmail

# Environment
NODE_ENV=development

# JWT Secret (generar con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=tu-jwt-secret-criptograficamente-seguro-de-128-caracteres

# CORS - Dominios permitidos (separados por coma)
CLIENT_URLS=http://localhost:3000,http://localhost:3001

# ============================================
# INSTRUCCIONES
# ============================================
# 1. Copiar este archivo como .env
# 2. Reemplazar todos los valores con tus credenciales reales
# 3. NUNCA commitear el archivo .env al repositorio
# 4. Para generar JWT_SECRET seguro:
#    node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# 5. Para obtener EMAIL_PASS de Gmail:
#    - Ir a Google Account > Security > 2-Step Verification
#    - App Passwords > Generar nueva contraseña
